# 历史初始化完成报告（2026-09-09）

## 最终结论

初始化活动 `#66` 已于 2026-09-09 11:04:45（Asia/Shanghai）完成：

- 状态：`completed`
- 历史范围：1990-12-19 至 2026-09-05
- 七个初始化阶段：全部执行结束
- 最终覆盖审计：10/10 `complete`
- 缺失分区：0
- 部分分区：0
- 未处理失败：0
- 运行模式：已原子切换为 `daily`
- 完成门禁：`all_phases_executed`、`strict_coverage_verified`、
  `daily_mode_activated` 全部为 `true`

“全历史初始化”按数据集契约解释。股票日线、每日基本面、资金流、涨跌停和财务
报表按各自可用起点回填；需要高基数实体扇出的接口按安全、可验证的基线窗口执行。
例如 `cyq_chips` 本轮基线为最近31个自然日，不代表从2018年穷尽全部筹码分布。

## 本轮阻塞与修复

### `cyq_chips` 合法2000行被误判为截断

`600722.SH` 在 2026-08-06 至 2026-09-05 返回恰好2000行。原完整性保护无法区分
合法整齐结果与静默截断，因此按设计失败关闭，导致扇出第17页停在199/200。

修复后，采集器遇到疑似上限会丢弃该响应并递归拆分日期范围。真实重试将范围拆为：

- 2026-08-06 至 2026-08-21：1297行
- 2026-08-22 至 2026-09-05：703行

两个叶子窗口都通过边界和返回日期验证，合计2000行，任务记录了
`adaptive_date_window_exhaustion`、2个叶子范围和3次请求证据。扇出随后完成
5556/5556个标的、28/28页，无最终失败。

### 季度财报错误套用日频审计跨度限制

最终验收最初在第7个数据集停止，原因是“一个审计最多3660天”的日频保护被错误
用于季度报告。修复后仅 `REPORT_QUARTERLY` 策略允许跨越10年以上；所有日频、
周频、月频和观察型审计继续保留3660天上限。季度范围只生成约143个报告期，不会
制造逐日分区或无界任务。

### 数据集名与上游接口名不一致导致证据漏关联

`financial_indicator` 数据集实际由 `fina_indicator` 接口采集。7个早期季度已保存
分页穷尽后的可信空结果，但旧审计按数据集名查任务，错误报告为缺失。现已在覆盖
契约中显式保存上游接口名，并同时补齐以下映射：

- `stock_daily` → `daily`
- `stock_daily_basic` → `daily_basic`
- `stock_limit` → `stk_limit`
- `financial_indicator` → `fina_indicator`

修复后的真实审计识别出143/143个财务指标季度，其中7个为已验证空分区，最终状态
`complete`，覆盖率100%。这套映射也保证将来的不完整任务不会被已有物理行掩盖。

## 验证

- 真实 Tushare 无写入验证：2000行、3次请求、2个叶子窗口、`verified=true`
- 全量测试（含 PostgreSQL 契约与 API 集成）：418项通过
- 初始化完成后，PostgreSQL 契约测试不再依赖“活动初始化”跳过口径
- 最终 `financial_indicator` 审计：143/143、missing=0、partial=0、coverage=1.0
- Docker 中 API、Scheduler、Worker、Auditor、PostgreSQL 和备份服务均健康

为缩短最后1956个标的的处理时间，收尾阶段临时扩展到3个 backfill Worker；全量
扇出完成后已恢复为单 Worker。全局 Tushare 限频、持久任务锁和网络故障资源退避在
扩容期间仍然生效。
